services:
  ntfy:
    container_name: "ntfy"
    image: binwiederhier/ntfy
    command:
      - serve
    environment:
      - TZ=GMT+1
    volumes:
      - ./var/cache/ntfy:/var/cache/ntfy
      - ./etc/ntfy:/etc/ntfy
    ports:
      - 8003:80
    restart: unless-stopped
