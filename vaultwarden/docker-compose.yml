services:
  vaultwarden:
    image: vaultwarden/server:latest
    container_name: vaultwarden
    restart: unless-stopped
    environment:
      DOMAIN: "https://vault.raffaele-terracciano.it"
    volumes:
      - ./data/:/data/
    ports:
      - 8007:80
